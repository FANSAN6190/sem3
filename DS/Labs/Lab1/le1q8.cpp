//8.WAP to find a word in given statement.
#include<iostream>
#include<string.h>
using namespace std;
string stmt,wd;
int wr(int i){
    if(wd[0]==stmt[i]){
        for(int j=0;wd[j]!='\0';j++){

        }
    }
}
int main()
{
    getline(cin,stmt);
    cin>>wd;
    //int l=strlen(wd);
    for(int i=0;stmt[i]!='\0';i++){
        if(wd[0]==stmt[i]){
            for(int j=0;wd[j]!='\0';j++){

            }
        }
        //cout<<stmt[i]<<"   "<<i<<endl;    
    }
}